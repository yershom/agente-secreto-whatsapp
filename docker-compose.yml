services:
  agente-wa:
    build: .
    container_name: agente-wa
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./conversations:/app/conversations
      - ./auth_info:/app/auth_info
      - ./.env:/app/.env:ro
    environment:
      - NODE_ENV=production
      - PORT=3000
